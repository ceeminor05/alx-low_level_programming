#include <stdio.h>
/**
 * main - prints single and taken digits
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf(num);
	}
	putchar('\n');

	return (0);
}
